#include <iostream>
#include <cstring>
using namespace std;
/*
const char* str_match(const char* s1, const char* s2)
{
	for (const char* arr = s1; !(*arr == '\0'); arr++)
	{
		const char* arrs1 = arr;
		const char* arrs2 = s2;
		while (*arrs1 == *arrs2 )
		{
			arrs1++;
			arrs2++;
		}
		if (*arrs1 == '\0' && *arrs2 == '\0')
		{
			return arr;
		}
		if (*arrs2 == '\0')
		{
			return arr;
		}
	}
	return nullptr;
}

int main()
{
	char s1[32];
	char s2[32];
	cout << "> " << flush;
	cin >> s1;
	cout << "> " << flush;
	cin >> s2;
	const char* arr = str_match(s1, s2);
	if(arr == nullptr)
	{
		cout << "null" << endl;
	}
	else
	{
		cout << arr << endl;
	}
}
*/
